# Generated by network generator
from keras.models import Model
from keras.layers import Input
from keras.layers import Dense
from keras.layers import Conv1D
from keras.layers import Flatten
from keras.layers import Dropout
from keras.layers.merge import concatenate

layer_000 = Input(shape=(28,28))
layer_001 = Conv1D(3, 3, activation='sigmoid')(layer_000)
layer_004 = Conv1D(1, 3, activation='relu')(layer_001)
layer_005 = Conv1D(18, 3, activation='sigmoid')(layer_004)
mnist_block01 = Flatten()(layer_005)
mnist_block02 = Dense(518, activation="relu")(mnist_block01)
mnist_block03 = Dropout(0.8280818)(mnist_block02)
layer_049 = Dense(518, activation="softmax")(mnist_block03) # this is the last layer of mnist block
layer_last = Dense(10, activation='sigmoid')(layer_049)
model = Model(inputs=layer_000, outputs=layer_last)